# --- Secrets / environment -------------------------------------------------
# NEVER commit real credentials or tokens.
.env
.env.*
!.env.sample

# Local MCP server config may contain real endpoints/keys via ${VARS}.
mcp.json

# Persisted API usage counters (runtime state).
usage.json

# --- Python ----------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# --- Tooling caches --------------------------------------------------------
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# --- Editors / OS ----------------------------------------------------------
.idea/
.vscode/
*.swp
Thumbs.db
.DS_Store

# --- Local runtime data ----------------------------------------------------
logs/
*.log
